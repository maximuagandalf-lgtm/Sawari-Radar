services:
  - type: web
    name: sawari-radar
    runtime: node
    plan: free
    buildCommand: npm --prefix server install && npm --prefix client install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
